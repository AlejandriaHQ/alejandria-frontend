import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alejandriahq.alejandria',
  appName: 'AlejandriaHQ',
  webDir: 'www'
};

export default config;
